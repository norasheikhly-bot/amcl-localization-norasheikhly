# generated from rosidl_cmake/cmake/rosidl_cmake_aggregate_target-extras.cmake.in

# Create a convenience aggregate target delivery_mission_interfaces::delivery_mission_interfaces
# that links all generated interface targets, so downstream packages can use
# a single modern CMake target name instead of ${delivery_mission_interfaces_TARGETS}.
if(delivery_mission_interfaces_TARGETS AND NOT TARGET delivery_mission_interfaces::delivery_mission_interfaces)
  add_library(delivery_mission_interfaces::delivery_mission_interfaces INTERFACE IMPORTED)
  set_target_properties(delivery_mission_interfaces::delivery_mission_interfaces PROPERTIES
    INTERFACE_LINK_LIBRARIES "${delivery_mission_interfaces_TARGETS}")
endif()
